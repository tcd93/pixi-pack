{
  "name": "pixi-prototype",
  "version": "1.0.0",
  "description": "A pixie prototype project (2 men 1 ball)",
  "scripts": {
    "test": "jest",
    "dev": "parcel src/index.html",
    "build": "npm run clean && parcel build src/index.html --public-url ./",
    "clean": "rimraf ./dist ./.cache"
  },
  "license": "MIT",
  "dependencies": {
    "pixi.js": "~5.2.2"
  },
  "devDependencies": {
    "@types/jest": "^24.0.25",
    "jest": "~24.9.0",
    "parcel-bundler": "~1.12.4",
    "ts-jest": "~24.2.0",
    "typescript": "^3.7.5",
    "rimraf": "^3.0.2"
  }
}
