import bomberFront from './images/front/*.png';
import bomberBack from './images/back/*.png';
import bomberRight from './images/right/*.png';
import bomberLeft from './images/left/*.png';

export const bomberFrames = {
    front: Object.values(bomberFront),
    back: Object.values(bomberBack),
    right: Object.values(bomberRight),
    left: Object.values(bomberLeft),
};